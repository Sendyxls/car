[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT --timeout 120 --access-logfile - --error-logfile -"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/"
timeout = 30
